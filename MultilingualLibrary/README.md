# Multilingual Library Examples

This example shows how to export i18n configuration and translation from a library as CommonJS module and how to avoid configuration overrides by 3rd party applications.